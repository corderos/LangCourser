@model IEnumerable<ISBD_project.Models.Language>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Language
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idL }) |
            @Html.ActionLink("Details", "Details", new { id=item.idL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idL })
        </td>
    </tr>
}

</table>
